# Configuración de Base de Datos para Producción (MariaDB)
DB_HOST=localhost
DB_USER=reposteria_user
DB_PASSWORD=TU_PASSWORD_SEGURA_AQUI
DB_NAME=la_reposteria
DB_PORT=3306

# Configuración del Servidor Principal (Frontend)
PORT=3000
NODE_ENV=production

# Configuración JWT - Servidor Principal
JWT_SECRET=CAMBIAR_POR_CLAVE_SUPER_SEGURA_PRODUCCION_2024
JWT_EXPIRES_IN=24h

# URLs de las APIs (localhost porque están en la misma Raspberry)
API_PRODUCTOS_URL=http://localhost:3001
API_CARRITO_URL=http://localhost:3002

# IMPORTANTE PARA PRODUCCIÓN:
# 1. Cambia TODAS las contraseñas y secrets
# 2. Usa contraseñas fuertes y únicas
# 3. NO subas este archivo a Git
# 4. En la Raspberry: cp .env.production .env
