# Configuración de Base de Datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=la_reposteria
DB_PORT=3306

# Configuración del Servidor Principal (Frontend)
PORT=3000
NODE_ENV=development

# Configuración JWT - Servidor Principal (DEPRECADO - Usar APIs individuales)
# Las APIs separadas tienen sus propios JWT_SECRET en sus carpetas
JWT_SECRET=M41n_S3rv3r_R3p0st3r14_1n1m4g1n4bl3_2024_L3g4cy
JWT_EXPIRES_IN=24h

# URLs de las APIs
API_PRODUCTOS_URL=http://localhost:3001
API_CARRITO_URL=http://localhost:3002

# IMPORTANTE: 
# 1. Copia este archivo como .env (sin el .example)
# 2. Cambia DB_PASSWORD con tu contraseña de MySQL
# 3. Cambia JWT_SECRET por una clave única y segura
# 4. NO subas el archivo .env a Git (ya está en .gitignore)
